# 问题总结

本文总结本对话窗口中，项目在升级生产级 RAG、PromptOps、Qwen/OpenAI 兼容配置、Chroma 索引、舆情 Agent 与推荐 Agent 过程中遇到的问题、原因、处理方案和后续建议。

## 1. 虚拟环境与依赖问题

### 现象

早期运行后端 unittest 时提示 bundled Python 缺少 Flask，且项目 `.venv` 曾被误判为不可用。用户确认实际虚拟环境位于：

```text
D:\毕业设计\.venv
```

并已安装 Flask。

### 原因

- Codex 内置 Python 与项目虚拟环境不是同一个解释器。
- 项目需要使用 `D:\毕业设计\.venv\Scripts\python.exe`。
- Windows 中文路径在部分命令输出中会显示乱码，但不代表环境不可用。

### 解决方案

后端测试和启动优先使用项目虚拟环境：

```powershell
cd D:\毕业设计\project
..\.venv\Scripts\python.exe -m unittest tests.test_agent_api_unittest tests.test_rag_api_unittest
```

已验证：

```text
Ran 8 tests
OK
```

## 2. Chroma 安装与依赖冲突

### 现象

安装 `chromadb==0.5.23` 后成功安装，但 pip 提示依赖冲突：

```text
transformers 5.5.4 requires huggingface-hub>=1.5.0
transformers 5.5.4 requires tokenizers>=0.22.0
chromadb==0.5.23 installed huggingface-hub==0.36.2 and tokenizers==0.20.3
```

### 原因

`chromadb==0.5.23` 依赖较旧版本的 `tokenizers` 和 `huggingface-hub`，与当前环境中的 `transformers 5.5.4` 不兼容。

### 建议方案

项目当前 RAG 不强依赖本地 Transformers embedding 模型，优先使用远程 Qwen/OpenAI embedding API。因此更稳妥的方案是：

- 保留 Chroma 依赖；
- 不强行升级 `tokenizers/huggingface-hub` 去满足 Transformers 5；
- 如必须使用 Transformers，则将 Transformers 降到兼容版本，例如 4.x；
- 用 `pip check` 验证最终依赖状态。

## 3. Chroma 索引尚未生成

### 现象

曾检查到：

```text
project/data/chroma 不存在
```

说明 Chroma 包即使安装完成，也不代表已经生成向量索引。

### 原因

安装 Chroma 只是安装向量数据库依赖；真正的索引需要调用项目的 RAG 重建接口或脚本。

### 生成方式

后端启动并登录后，可通过接口或前端 RAG 评测看板触发：

```http
POST /api/rag/index/rebuild
POST /api/rag/index/anime/<anime_id>
GET  /api/rag/index/status
```

也可以通过项目提供的索引脚本重建，具体以当前项目脚本为准。

### 注意

切换 embedding provider 或 embedding model 后，旧 collection 不应继续混用，必须新建 collection 并重建索引。

## 4. RAG 检索降级为关键词模式

### 现象

Agent 步骤里出现：

```text
rag_fallback_keyword success live_database returned 3 evidence items
```

### 原因

这不是错误。它表示当前没有使用 Chroma 向量检索，而是降级使用数据库关键词/统计型证据。

常见原因：

- 未配置 embedding API key；
- 尚未生成 Chroma 索引；
- active collection 不存在；
- active collection 的 embedding provider/model 与当前配置不一致；
- Chroma collection count 为 0。

### 解决方案

- 配置 embedding 环境变量；
- 重启后端；
- 重建索引；
- 检查 `/api/rag/index/status` 中 active collection、文档数和模型元数据；
- 确认检索步骤变为 `rag_retrieve` 或显示 via chroma。

Qwen embedding 推荐配置：

```powershell
$env:EMBEDDING_PROVIDER="qwen"
$env:EMBEDDING_BASE_URL="https://dashscope.aliyuncs.com/compatible-mode/v1"
$env:EMBEDDING_MODEL="text-embedding-v4"
$env:EMBEDDING_API_KEY="你的 DashScope API Key"
```

## 5. 舆情 Agent 出现 LangGraph 递归上限

### 现象

前端步骤中出现：

```text
langchain_opinion_agent fallback
Recursion limit of 6 reached without hitting a stop condition
```

### 原因

舆情 Agent 已经在进入 LLM 前预取了全部工具数据：

- anime info；
- sentiment stats；
- sentiment trend；
- topics；
- wordcloud；
- representative comments；
- aspect sentiment；
- Bangumi info；
- RAG evidence。

但旧实现又把工具列表交给 LangGraph Agent，模型可能继续规划并重复调用工具，导致递归上限内没有进入最终结构化输出。

### 已实施方案

将舆情报告生成从“工具调用 Agent”改为“直接结构化生成”：

```text
预取工具数据
-> 检索 RAG evidence
-> 构造 prompt
-> 单次结构化报告生成
-> 后端补齐 evidence、prompt_trace、agent_steps
-> 失败时本地 fallback
```

关键点：

- 不再把 `OPINION_TOOLS` 交给最终 LLM 生成阶段；
- 最终步骤命名为 `structured_opinion_report`；
- 避免 LangGraph 工具循环。

## 6. 舆情 Agent 结构化报告生成超时

### 现象

前端显示：

```text
structured_opinion_report fallback Request timed out.
fallback_opinion_report fallback 使用本地统计模板生成舆情报告
```

### 原因

LLM 单次结构化生成需要处理较大的上下文，包括统计、趋势、主题、词云、评论、Bangumi 简介和 RAG evidence。上下文太长时，Qwen/OpenAI 兼容接口容易超时。

### 已实施方案

1. `backend/config.py` 增加统一 LLM 超时配置：

```text
LLM_TIMEOUT
LLM_MAX_RETRIES
```

2. `backend/agents/model_factory.py` 改为读取环境变量，而不是写死 timeout。

3. `backend/agents/opinion_agent.py` 增加紧凑上下文：

- 趋势只保留关键点；
- 主题只保留 top N 关键词；
- 词云截断；
- 每类代表评论限制条数和长度；
- RAG evidence 限制条数和长度；
- Bangumi summary 截断。

4. 增加二级重试链路：

```text
structured_opinion_report
-> structured_opinion_report_compact_retry
-> fallback_opinion_report
```

## 7. OpenAI API Key 无效导致舆情 Agent fallback

### 现象

前端显示：

```text
Error code: 401
Incorrect API key provided
invalid_api_key
```

### 原因

后端当时实际读取的是 OpenAI 配置：

```text
LLM_PROVIDER=openai
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-5.4-mini
LLM_API_KEY=sk-cca5d...01c2
OPENAI_API_KEY=sk-proj-...Z04A
```

其中 `LLM_API_KEY` 优先级高于 `OPENAI_API_KEY`，而 `LLM_API_KEY` 对 OpenAI 无效，所以触发 401。

### 结论

这类错误不是超时，也不是 RAG 问题，而是 LLM provider 与 API key 不匹配。

### 建议方案

- 认证错误不应进入 compact retry，因为同一个错误 key 重试必然失败；
- 后续可在代码中增加 `invalid_api_key/401/unauthorized` 的不可重试判断；
- 前端步骤中可显示 `llm_config_check`，提示当前 provider/model/base_url/key 是否存在。

## 8. Qwen/OpenAI 环境变量冲突

### 现象

虽然 `config.py` 默认改成了 Qwen：

```python
LLM_PROVIDER = os.environ.get("LLM_PROVIDER", "qwen")
```

但新终端里仍显示：

```text
openai
https://api.openai.com/v1
gpt-5.4-mini
```

### 原因

`config.py` 的默认值只有在环境变量不存在时才生效。当前 IDE/Codex/PowerShell 父进程的 Process 环境变量里已经存在：

```text
LLM_PROVIDER=openai
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-5.4-mini
```

Process 环境变量优先级高于 User 环境变量，也高于代码默认值。

### 已处理

已写入 User 级 Qwen 配置：

```text
LLM_PROVIDER=qwen
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL=qwen3-8b
LLM_API_KEY=sk-cca5d...01c2
OPENAI_API_KEY=<empty>
```

但当前已打开的 IDE/终端仍继承旧 Process 环境变量，因此需要完全重启 IDE/终端，或在启动后端前手动覆盖当前终端变量。

### 当前终端临时切换命令

```powershell
$env:LLM_PROVIDER="qwen"
$env:LLM_BASE_URL="https://dashscope.aliyuncs.com/compatible-mode/v1"
$env:LLM_MODEL="qwen3-8b"
$env:LLM_API_KEY="你的 Qwen/DashScope API Key"
Remove-Item Env:OPENAI_API_KEY -ErrorAction SilentlyContinue
```

然后重启 Flask 后端。

## 9. 是否存在完整 Qwen API Key

### 检查结果

当时检查三层环境变量：

```text
Process
User
Machine
```

发现：

- `DASHSCOPE_API_KEY` 为空；
- `QWEN_API_KEY` 为空；
- `LLM_API_KEY` 存在，用户确认希望将其作为 Qwen key 使用；
- `OPENAI_API_KEY` 存在于旧 Process 环境中，造成冲突。

### 处理原则

- 不把完整 API key 写入源码；
- 文档只记录脱敏值；
- 后端启动进程必须显式使用 Qwen provider/base_url/model/key。

## 10. 如何重启 Flask 后端

### 操作步骤

1. 在运行后端的 PowerShell 窗口按：

```powershell
Ctrl + C
```

2. 进入项目目录：

```powershell
cd D:\毕业设计\project
```

3. 激活虚拟环境：

```powershell
D:\毕业设计\.venv\Scripts\Activate.ps1
```

若脚本策略阻止：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
D:\毕业设计\.venv\Scripts\Activate.ps1
```

4. 确认环境变量：

```powershell
echo $env:LLM_PROVIDER
echo $env:LLM_BASE_URL
echo $env:LLM_MODEL
```

期望为：

```text
qwen
https://dashscope.aliyuncs.com/compatible-mode/v1
qwen3-8b
```

5. 启动后端：

```powershell
python run.py
```

或根据项目入口使用：

```powershell
python backend/app.py
python -m backend.app
```

## 11. model_factory.py 的 Qwen/OpenAI 兼容修改

### 问题

Qwen 的 OpenAI 兼容接口需要传：

```python
extra_body={"enable_thinking": False}
```

但 OpenAI 官方接口不支持这个参数。如果切换到 OpenAI 时仍传该字段，可能导致请求异常。

### 已实施方案

`backend/agents/model_factory.py` 中根据 provider 判断：

```python
if LLM_PROVIDER == "qwen":
    kwargs["extra_body"] = {"enable_thinking": False}
```

这样：

- Qwen 使用 `enable_thinking=False`；
- OpenAI/Zhipu 不传 Qwen 专用参数；
- 保持 OpenAI-compatible 架构。

## 12. RAG 与 PromptOps 生产级升级已完成的主要内容

本窗口之前已经完成或讨论过的核心升级包括：

- 新增 `backend/rag/` 模块；
- 新增 embedding 客户端配置；
- 新增 Chroma vector store 封装；
- 新增异步索引任务表和接口；
- 新增 RAG search API；
- 新增评测 API 和评测看板；
- 新增 Prompt registry 和模板；
- Agent 输出增加：
  - `retrieval_evidence`
  - `evidence_refs`
  - `prompt_trace`
  - `agent_steps`
- 前端 Agent 页面展示证据链、来源、相似度、prompt 版本和执行步骤；
- 无 API key 时自动降级到关键词/统计型证据和本地 fallback。

## 13. 手动清理旧数据的原则

### 原则

项目约束明确禁止批量删除文件或目录，不能使用：

```text
del /s
rd /s
rmdir /s
Remove-Item -Recurse
rm -rf
```

如果需要删除文件，只能一次删除一个明确路径的文件。

### 可考虑手动清理的内容

- 不再需要的旧数据库备份，例如：

```text
D:\毕业设计\project\data\anime_sentiment_backup_20260506_020708.db
```

- 后续如果生成多个旧 Chroma collection，不应由程序批量删除，建议用户确认后手动清理。

### 不建议删除

- 当前主数据库；
- `frontend/dist`，除非确认要重新构建；
- `node_modules`，除非准备重新安装依赖；
- raw/processed/train 数据目录，除非确认已备份。

## 14. 当前仍需关注的问题

1. 当前 IDE/终端是否仍残留 OpenAI 的 Process 环境变量。
2. Flask 后端是否已经完全重启并读取 User 级 Qwen 配置。
3. `config.py` 的 API key 读取逻辑是否进一步改为 provider 隔离。
4. 401/invalid_api_key 是否加入不可重试判断。
5. Chroma 是否完成真实向量写入，而不是只有 SQLite 任务状态成功。
6. active collection 的 embedding provider/model 是否与当前配置一致。
7. 舆情 Agent 在真实 Qwen key 下是否还能稳定通过 `structured_opinion_report` 或 compact retry。
8. 前端长步骤名是否需要做显示别名，避免页面挤压。

## 15. 推荐后续改进

### 配置层

按 provider 分流读取 key，避免 OpenAI key 被误用于 Qwen：

```python
if LLM_PROVIDER == "qwen":
    LLM_API_KEY = os.environ.get("LLM_API_KEY") or os.environ.get("DASHSCOPE_API_KEY") or os.environ.get("QWEN_API_KEY") or ""
elif LLM_PROVIDER == "openai":
    LLM_API_KEY = os.environ.get("LLM_API_KEY") or os.environ.get("OPENAI_API_KEY") or ""
elif LLM_PROVIDER == "zhipu":
    LLM_API_KEY = os.environ.get("LLM_API_KEY") or os.environ.get("ZHIPU_API_KEY") or ""
```

### Agent 层

- 增加 `llm_config_check` 步骤；
- 对 `401/invalid_api_key/unauthorized` 直接 fallback，不再 compact retry；
- 舆情 Agent 可进一步使用轻量 LLM 输出 schema，由后端补齐诊断字段；
- 对 prompt 长度、evidence 数量、输出 token 设置更严格预算。

### RAG 层

- 重建索引时必须检查 Chroma collection count；
- active collection 必须保存 embedding provider/model/dimension 元数据；
- 检索前检查模型是否匹配；
- 旧 collection 保留，但不自动批量删除。

### 前端层

- 将 `structured_opinion_report_compact_retry` 显示为较短别名；
- hover 或详情中展示完整 step name；
- 对 `rag_fallback_keyword` 使用“降级可用”而不是“失败”的视觉语义；
- 对 API key 错误给出更明确的配置提示。
