# ============================
# 面向动漫评论的情感分析与舆情监控系统
# Python 依赖列表（Python 3.12）
# ============================

# ─── Web 爬虫 ───────────────────────────────────────────
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0

# ─── 数据处理 ────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4

# ─── 中文 NLP ────────────────────────────────────────────
jieba==0.42.1
snownlp==0.12.3

# ─── 机器学习 ────────────────────────────────────────────
scikit-learn==1.5.2

# ─── 深度学习 ────────────────────────────────────────────
# CPU/CUDA 专用安装命令参考: https://pytorch.org/get-started/locally/
torch==2.11.0
transformers==4.46.3
# Transformers 4.46 与 Chroma 0.5.23 的兼容交集。
tokenizers==0.20.3

# ─── 主题建模 ────────────────────────────────────────────
gensim==4.4.0

# ─── Web 框架 ────────────────────────────────────────────
fastapi==0.139.0
uvicorn==0.51.0
# FastAPI TestClient 的直接测试依赖。
httpx==0.28.1

# ─── 数据库 ORM / 迁移 ───────────────────────────────────
# FastAPI 请求使用 AsyncSession + aiomysql；Agent/脚本使用同步 PyMySQL。
SQLAlchemy==2.0.51
aiomysql==0.3.2
PyMySQL==1.2.0
alembic==1.18.5

# ─── 工具库 ──────────────────────────────────────────────
tqdm==4.66.5

# ─── 用户认证 ────────────────────────────────────────────
PyJWT==2.12.1
bcrypt==5.0.0

# ─── LangGraph Agent Center ──────────────────────────────
# 与 Recommendation Agent 2.0、ToolNode 及 Redis/SQLite Checkpointer
# 的当前回归测试环境保持一致。
langchain==1.0.8
langchain-core==1.4.8
langchain-openai==1.0.3
langgraph==1.0.10
# 1.0.13 会读取 langgraph 1.0.10 尚未提供的 ExecutionInfo/ServerInfo。
langgraph-prebuilt==1.0.8
langgraph-checkpoint==4.1.1
langgraph-checkpoint-sqlite==3.1.0
langgraph-checkpoint-redis==0.5.1
aiosqlite==0.22.1
sqlite-vec==0.1.9
PyYAML==6.0.3
pydantic==2.12.4

# ─── Agent 分布式任务队列 ──────────────────────────────────
# Redis 同时作为 Celery broker/result backend；业务任务状态仍以 SQL 表为准。
celery[redis]==5.6.2

# ─── RAG 向量存储 ────────────────────────────────────────
# Chroma 在运行时可选；不可用时自动降级为 SQLite 关键词检索。
chromadb==0.5.23

