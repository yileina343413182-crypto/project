# 修改说明

本次修改未重建虚拟环境，保留现有依赖目录和运行数据，仅修正代码、配置与文档层面的可维护性问题。

## 修改内容

1. 修正启动脚本的模型检查
   - `run.py` 现在检查实际保存/加载使用的模型文件：
     - `models/saved/textcnn/textcnn_model.pt`
     - `models/saved/textcnn/textcnn_vocab.pkl`
     - `models/saved/textcnn/textcnn_config.json`
     - `models/saved/bert/bert_sentiment_model.pt`
     - `models/saved/bert/classifier_config.json`
     - `models/saved/bert/config.json`
   - 避免出现“模型不存在，但接口实际可预测”的误报。

2. 配置改为环境变量优先
   - `backend/config.py` 新增环境变量读取：
     - `FLASK_DEBUG`
     - `FLASK_HOST`
     - `FLASK_PORT`
     - `CORS_ORIGINS`
     - `LLM_API_KEY`
     - `JWT_SECRET_KEY`
     - `JWT_ACCESS_TOKEN_EXPIRES`
   - 移除了代码中的默认 LLM API Key。
   - `LLM_API_KEY` 为空时，推荐模块继续降级为本地模糊匹配。
   - 生产模式关闭调试时必须配置 `JWT_SECRET_KEY`。

3. 优化备用启动脚本
   - `run_server.py` 不再写死 `debug=True`、端口和监听地址。
   - 支持参数：
     - `--host`
     - `--port`
     - `--debug`
     - `--no-debug`

4. 优化前端加载和构建分包
   - `frontend/src/router/index.js` 将页面组件改为路由懒加载。
   - `frontend/vite.config.js` 增加 `manualChunks`，拆分：
     - `vue`
     - `charts`
     - `http`
   - 将 Vite chunk 提醒阈值调整为 1200 KB；当前较大的包主要来自独立的 ECharts 图表依赖，不再混入首屏业务入口。

5. 新增自动化冒烟测试
   - 新增 `tests/test_api_unittest.py`。
   - 使用 Flask test client，无需手动启动后端服务。
   - 覆盖：
     - `/api/health`
     - `/api/anime/list`
     - 404 响应格式

## 常用命令

后端启动：

```powershell
cd D:\毕业设计\project
python run.py
```

备用后端启动：

```powershell
python run_server.py --host 127.0.0.1 --port 5000 --no-debug
```

前端开发：

```powershell
cd D:\毕业设计\project\frontend
npm.cmd run dev
```

前端构建：

```powershell
npm.cmd run build
```

后端自动化测试：

```powershell
cd D:\毕业设计\project
python -m unittest discover -s tests
```

## 推荐环境变量示例

```powershell
$env:FLASK_DEBUG="true"
$env:FLASK_HOST="0.0.0.0"
$env:FLASK_PORT="5000"
$env:CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"
$env:JWT_SECRET_KEY="replace-with-a-long-random-secret"
$env:LLM_API_KEY=""
```

生产或答辩演示前建议至少配置 `JWT_SECRET_KEY`，如需 AI 推荐再配置 `LLM_API_KEY`。
